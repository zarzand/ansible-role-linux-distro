---

- name: Restart etc hosts
  ansible.builtin.service:
    name: hostsd
    state: restarted

- name: Restart sshd
  ansible.builtin.service:
    name: sshd
    state: restarted

- name: Reboot Server
  ansible.builtin.reboot:

- name: Generating Netplan Configuration
  ansible.builtin.command: netplan generate
  become: true
  #listen: netplan generate config
  notify:
    - Applying Netplan Configuration #netplan apply config


- name: Applying Netplan Configuration
  ansible.builtin.command: netplan apply
  become: true
  #listen: netplan apply config
  #when: netplan_apply
